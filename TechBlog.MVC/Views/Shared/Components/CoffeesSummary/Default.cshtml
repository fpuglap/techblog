@model CoffeesViewModel

@if (Model.Coffees.CoffeesBuyed.Count > 0)
{
    <li>
        <a>
            @*<span class="glyphicon glyphicon-shopping-cart"></span>*@
            <span id="coffees-status">
                @Model.Coffees.CoffeesBuyed.Count
            </span>
        </a>
    </li>
}